FROM node:26-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --omit=dev

COPY server ./server

ENV NODE_ENV=production
ENV DATA_DIR=/data
EXPOSE 3001

CMD ["node", "--experimental-sqlite", "server/index.js"]
